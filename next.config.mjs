/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  trailingSlash: true,
  images: {
    unoptimized: true
  },
  // Явно указываем distDir
  distDir: 'out',
}

export default nextConfig
